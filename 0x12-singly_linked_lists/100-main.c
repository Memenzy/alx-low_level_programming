#include <stdio.h>

/**
 * main - check point 
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("(A tortise, having pretty good sense of a hare's nature,"
		" challenges one to a race.)\n");
	return (0);
}
